# -*- coding: utf-8 -*-
"""さっき書いた「杉山清貴は1枠不足」は誤りだったので直す（2026-09-05）。

実ページ eventCd=2627269 を生HTMLで確かめたら:
  ・「ぴあカードで確率UP」は**販促ラベル**で、抽選枠ごとに出る（3枠＋凡例で4か所）
  ・「当選確率アップ」は**レスポンシブの複製側**に1か所だけ（売り場コードが近くに無い）
  ・売り場コードは 28827(3次受付・受付中) / 22049(受付終了) / 10580(2次受付・受付終了) の**3つだけ**
＝**別券種ではない。取りこぼしは無かった。**
"""
import io

P = ('C:/Users/user/.claude/projects/C--Users-user-oshinavi/memory/'
     'feedback_pia_parser_flattens_slots.md')
s = io.open(P, encoding='utf-8').read()

OLD = """- 登録19枠と突き合わせると、**足りないのは1枠だけ**だった
  （「ぴあカードで確率UP」と「当選確率アップ」の**3次受付が2券種**あるのに、`kenshu()` が
  両方「3次受付」に潰すので `merge_apply` が足さない）
"""
NEW = """- 登録19枠と突き合わせると **取りこぼしは無かった**（＝「10枠不足」は数え方の誤り）

🚨🚨**その数え方にも穴があった＝あたしは最初「3次受付が2券種ある＝1枠不足」と報告したが、誤りだった。**
状態語を含むブロックで行を切ると、**販促ラベル**と**レスポンシブの複製**が別枠に見える：
- 「**ぴあカードで確率UP**」は券種名ではなく**販促ラベル**。抽選枠ごとに出る（3枠＋凡例で4か所）
- 「**当選確率アップ**」は**複製側**に1か所だけ。近くに売り場コードが無い
  （複製側は「詳細はこちら」も無い＝見分けの印）
- このページの売り場コードは **28827(3次受付・受付中) / 22049(受付終了) / 10580(2次受付・受付終了) の3つだけ**

✅**行の数は当てにしない。決着は「その行の近くに売り場コードがあるか」と `parse_cards` の出力で付ける。**
雛形＝`tmp/check_2627269_0905.py`（キーワードの前後1800字から `ticketInformation.do?...Cd=` を拾う）。
別セッションの `tmp/cards_dump_0905.py` / `tmp/status_ctx_0905.py` も同じことをする。
"""

assert OLD in s, 'target not found'
io.open(P, 'w', encoding='utf-8', newline='\n').write(s.replace(OLD, NEW, 1))
print('FIXED')
