# OSHINAVI作業プラン

毎朝の作業開始時、必ずこのファイルを先に確認してください。
🧭 **何か作業する前に必ず `PLAYBOOK.md` の該当行を見る → 挙がった memory を全文Read → 動く**（一行要約で動かない）。

---

# 🔴 いま止まっているところ（9/10 朝の便のあと・/clear 直後はここから）

```
✅ 朝の便は終わってpush済み（04c34196）＝ルーチン／削除19／振り分け148／新着95投入
🕐 背景タイマー＝12:10（昼のヒール）と 17:00（夜の便）に自分を呼び戻す仕掛けが生きている
   ※今日の当日発売は 10:00/10:30/11:00/12:00 と **18:00・21:00**。夜の分も忘れない

⏳ ユーザーの返事待ち（**もらうまで聞き続ける**）
   1. 新着タブの**楽天20件を振り分けていいか**
      （ユーザー9/10「今乗ってる楽天チケットは目視で確認できた／目視で確認できるのは公演日だけ」）
   2. e+の2件（7706 藤井風ピアノリサイタル／7707 はなもも）を振り分けていいか
   3. id7498 CDジャーナル連載10周年コンサート秋のジャンル＝musicetc か talkshow か
      （楽天のURLが /event/ だけでジャンルの手がかりが無い＝売り場が言っていないので決められない）
   4. Xのアナリティクスから投稿ごとのCSVを落としていいか（リンククリック数はCSVにしかない）
```

# ✅ 9/10 に片づいたこと（楽天まわり・ユーザー指摘2件から）

```
ユーザー＝①「木下グループが売り切れ出てる　売り切れで表示してね」
          ②「楽天から持ってきたチケットが売ってるのしかない件　これから発売のチケット探して」

①🚨楽天の売り状態は**生HTMLに1文字も無い**（カードの class は売り切れても active のまま）。
   購入ボタンのAJAX（POST cms-api.ticket.rakuten.co.jp/coreui/ajax/performance/widget
   ／{ids, ecd, eid}・ecd と eid は生HTMLから正規表現で取れる）にしか出ない。
   → tools/rakuten_perf_status.py ＋ tools/rakuten_mark_soldout.py（朝ルーチン3.45）
   → 34件を当てて **10枠に「予定枚数終了」**（7494×5／3224×2／3228×2／3235／7498）
   ⏭️別レイアウトで**調べられていない9件**（＝売り切れていない、ではない）:
      1 さだまさし／3674 クーザ／7505 THE ORCHESTRA TOKYO／7506 Chalca／7507 ちゃーむぽっしゅ／
      7582 世田谷たまがわ花火／7583 春猿火／7584 KOKO／7585 理芽
   🎯おまけ＝APIの返りに**公演ごとの購入URL**が入っている。
      「ラフ×ラフの米子の昼だけ押させる」がこれで作れる（まだ焼き込んでいない）。

②🚨入口の答え＝**post-sitemap の番号は投稿順**（1番=2019年／26番=2025-03〜／27番=2026-04〜）。
   lastmod で絞ると 27,105件中119件しか見えない＝発売前が3枠しか無かった真因。
   → tools/rakuten_presale_harvest.py --maps 27,26（1,262件）
   🚨登録済みなのに**発売前の枠だけ抜けている**型がある（理芽・KOKO・春猿火で実証）。

③ぴあ側にも同じ穴＝mark_soldout はエントリ単位で「買える枠が1つでもあれば素通り」。
   → tools/pia_mark_soldout_slots.py（枠ごと）で5枠を打ち分けた。
   🚨締切がもう過ぎている枠には印を付けない（正しく消えているのに出し直すだけ）。

④朝の統合行き52件は**全部片づいた**（47件を合流／3件を実ページで見て合流／2件は別興行で新規投入）。
   キーウ(id942)の取りこぼし5枠も回収して MISSING 2→0。
```

# 🚨 今日踏んだ罠（同じ道を通らない）

```
- index.html を `newline=''` で読んで既定で書き戻すと `\r\r\n` になる（7,266か所）。
  読みも書きも「指定なし」で統一。**戻す先はバックアップでなく git**（バックアップも同じ変換を通る）。
- バッジに開演時刻を入れたら照合側の公演日の読み取りが割れた（`[\d/〜]+` が「13:00」の00を拾う）。
  **片側を変えたら同じ日に両側を直す。**
- 枠を足す時に**券種名で突き合わせると二重登録**になる＝ぴあは同じ枠を
  「一般発売（茨城 10/31公演）」と「一般発売【ノバホール公演】（茨城 10/31公演）」の2通りで出す。
  → **（県・公演日・締切）**で突き合わせる（tmp/add_missing_slots_0910.py）。
- **別エントリへ枠を移す時はURLを焼き込む**＝押した人が別会場のページへ飛ぶ（AKIHIDEで発生）。
- **dateLabel の会期を縮めない**＝後ろ側は「〜12月25日」と年を書かないことがある（id942 キーウ）。
  点検＝tmp/check_datelabel_shrink_0910.py。
- 会場(venue)を union で作り直さない＝駐車場チケットの会場名が混ざる（id4998）。
- 画面の不具合を報告する前に**押して数える＋その行のコメントを読む**
  （ミュージカルのボタンは仕様どおりだったのに「壊れている」と報告した）。
```

# 🚨 消さずに残した／直す必要があるもの

```
1. ヒールの削除候補 **72件**は**まだ消していない**（tmp/delete_candidates_0910.md に仕分け済み）
   ・他社あり 1件／**売切・販売終了フラグあり 59件＝消さない側**／締切不明 0件／
     ぴあだけ・要他社確認 **9件**／既に消えている 3件
   → 夜に見るのは**9件だけ**でいい。

2. id6109 第107回 三三・左龍の会＝ぴあがeventCdを無効化＝**削除NG**。後継URLを探す
     python tools/pia_kw_search.py "第107回 三三・左龍の会"

3. id7558 Tommy february6 のハロウィン上映会＝一般発売の枠がぴあから消えた。
   先行（神奈川10/31・〜10/20）は生きている。**振り分けずプールに残してある。**

4. id942 キーウの東京12/13（〜12/10）が STALE のまま＝別ページに移った疑い。

5. 【夜または明日】artist に公演名が入っていて検索に出てこないエントリ
   ・候補 **87件**（tmp/artist_romaji_0910.txt）＝「日本語ゼロ＋公演名らしい語」
   ・大半は本当に英語名のイベント。**ぴあの［出演］欄と突き合わせないと決まらない**
   ・実例は2つ見つかっている＝id4119 稲垣潤一（9/9ユーザー発見）／id5014 柚香光（9/10あたしが発見）

6. 「買える枠が0のまま1か月以上先の公演」が **67件**（node tools/check_zero_badge.js）。
   大半は「先行だけ登録されていて一般発売がまだ発表されていない」型。
   → 出し方（販売終了バッジを付けるか今のまま置くか）を夜の「お疲れ様」で聞く。
```
