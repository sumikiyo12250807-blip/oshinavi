# -*- coding: utf-8 -*-
"""plan.md の先頭に 8/8 の引き継ぎを差し込む（8/7 の「★最新」印は外す）。"""
import io
import shutil
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8", errors="replace")

P = r"C:\Users\user\oshinavi\plan.md"
raw = open(P, "rb").read()
crlf, lf = raw.count(b"\r\n"), raw.count(b"\n")
NL = "\r\n" if crlf else "\n"
print("plan.md 改行: CRLF %d / 単独LF %d" % (crlf, lf - crlf))
shutil.copyfile(P, P + ".bak_0808")

text = raw.decode("utf-8")
ANCHOR = "## 🌅 8/7 の引き継ぎ ★最新"
assert text.count(ANCHOR) == 1, "8/7の見出しが1つ見つからない"

NEW = """## 🌅 8/8 の引き継ぎ ★最新（index **2594件**／新着プール **0件**／push 本日**0回**／**未pushコミット2件＋未コミットあり**）

### 🧳 ユーザーは 8/8〜8/10 旅行中（温泉＋相撲巡業）
- 8/7 が**ユーザーの誕生日**だった。8/8朝から夫婦で温泉旅行、**8/9は相撲の巡業**（oshinavi.jp から買ったチケットで行く）。
- **旅行中はサイトを更新しない**と決めた。クラウド実行（/schedule）も検討したが、**ローカルPCを触れない＋無断pushがルール違反**になるので不採用。
- 🚨**PCは持っていかない/車に置かない**（夏の車内60℃でバッテリーが危険）と伝えてある。
- ✅**3日空けても壊れない**ことを実コードで確認して説明済み＝`index.html:83816` の
  `if ((!t.startDate || parseDateStr(t.startDate) <= today) && parseDateStr(t.date) < today) return "";`
  ＝**締切切れの枠(バッジ)は自動で消える**。ただし**カード(エントリ)自体は残る**ので、
  「中身が空のカード」と「取り逃した新着」が3日ぶん溜まるだけ。**嘘の情報は出ない**。

### 🔥 帰ってきたら（8/10夜 or 8/11朝）の段取り
1. **3日分をまとめて処理**＝期限切れチェック → 隠れ枠ヒール → 新着150件相当（rlsIn=03は30日以内発売なので取り逃さない）
2. **8/7から持ち越しの宿題**（下の「残っている宿題」）
3. **push**（8/7の1回目は済み・**8/8は0回**。未pushコミット2件＋未コミット分がある）

### ✅ 8/8 朝にやったこと＝X投稿の予約を代行（サイトの更新はしていない）
| 時刻 | 内容 | 状態 |
|---|---|---|
| 6:29 | スキマスイッチ①（関西4公演） | **投稿済み**＋**編集で修正済み** |
| 9:00 | スキマスイッチ②（秋の関西4本・別文面） | 予約 |
| 9:30 | 水瀬いのり（富士急コニファー2days） | 予約 |
| 9:45 | HY（全国14公演） | 予約（12:00→9:45に変更） |

- 本文は `tmp/x_posts_20260808.txt` / `tmp/x_post_0808_sukima2.txt` / `tmp/x_post_0808_sukima1_fix.txt`
- **未投稿で残してあるのが2本**＝`tmp/x_posts_20260809_10.txt`（④奥華子=8/10発売の予告／⑤シシド・カフカ=8/10本日発売）

### 🚨 8/8 の失敗と、そこから作ったルール（memory 3件を新設）
1. **[[feedback_x_kuten_kaigyo]]**＝X本文は「。」で必ず改行する。**ユーザーが毎回手で直していた**（「読んでもらうためなの？」）。詰める理由は無かった＝あたしの手抜き。
2. **[[feedback_x_browser_operation]]**＝**誤投稿を1件やった**。Confirm後にボタンが `Post`→`Schedule` に変わるのに、
   クリックが効かず座標だけ信じて押し直したら**その時には `Post` に戻っていて即時投稿**された。
   → **押す直前にボタンの文字を撮って確認する**／**文面を見せてOKをもらってから押す**（ユーザー「もう予約したの？」）。座標と手順も記録した。
3. **[[feedback_x_artist_fact_check]]**＝**スキマスイッチを「二人の声が重なる」と書いた**が、ユーザー「**あまりハモリはないよ**」。
   デュオ＝ハモる、という思い込み。**編成/パート/演奏スタイルは裏取りなしに書かない**。
   ✅ユーザー採用表現＝「**何回聴いても感動ものよ**」「**背中がぞわっとする**」／✅却下＝「最初のいっぱつ目みたいにくる」（意味が伝わらない）

### 🚧 残っている宿題（8/7から持ち越し・帰宅後に着手）
- **ツアー取りこぼし20件の育成**（`tmp/missing_audit_0807.txt`）＝トンボコープ5／Juice=Juice 3／THE MODS 3／松平健3／海援隊2／夜の本気ダンス2／TenTwenty 1／加藤ミリヤ1。**`--rls-from all`** で回すこと（[[feedback_grow_audit_rlsfrom_blindspot]]）。
- **3878 海援隊（長野12/19）に e+ プレオーダー(8/18 12:00〜8/25 18:00)** が未登録。
- **e+で見つけた別公演3件**＝Takahito Uchisawa from androp（11/8・受付中）／渡辺満里奈40th（ヒューリックホール/Billboard）／務川慧悟「夜ピアノ2026」（ミューザ川崎12/16）。
- **時速36km・Damian Hamada・山内総一郎**は e+ に全国ツアーの他会場が生きているのに1公演しか登録していない。
- **グルメジャンルの Amazon 汎用グッズリンク**が未設定（`GENRE_AMAZON_LINKS`）＝ユーザーから `amzn.to` をもらったら足す。
- **SUNOの曲＋H3の踊り動画**＝15秒が上限（音声も合計15秒まで）。歌詞は `tmp/` に15秒版を作ってある。SUNOで作った曲をDownloadsに置いてもらえば、あたしが15秒に切って踊らせる。
- 8/6から＝全エントリのバッジ検査45件の指摘／H3の声の固定は未テスト。

---

"""

text = text.replace(ANCHOR, NEW + "## 🌅 8/7 の引き継ぎ", 1)
if NL == "\r\n":
    text = text.replace("\r\n", "\n").replace("\n", "\r\n")
open(P, "wb").write(text.encode("utf-8"))
raw2 = open(P, "rb").read()
print("書き込み後: CRLF %d / 単独LF %d" % (raw2.count(b"\r\n"), raw2.count(b"\n") - raw2.count(b"\r\n")))
print("✅ plan.md に 8/8 の引き継ぎを追加した")
