# 削除・売り切れ 点検表（消す前にこの1枚を見る・2026-09-01版）

> 🚨**削除は不可逆。迷ったら消さない。**
> ここは「いま何をするか」だけ。**なぜそうなったか（事故の記録）はmemory側**にある＝
> [[feedback_user_confirms_expired]]／[[feedback_delete_nonpia_blindspot]]／[[feedback_soldout_keep_visible]]ほか。
> ルールが変わったら**この表とmemoryの両方**を直す（[[feedback_rules_one_place_and_fix_summaries]]）。

---

## 1. まず分ける＝これは「消す話」か「表示を変える話」か

| 実態 | どうする |
|---|---|
| **予定枚数終了（売り切れた）** | ❌消さない。`soldout: true` ＋ `soldoutSince`。バッジ「**予定枚数終了**」（実線） |
| **販売終了（期間が終わっただけ）** | ❌消さない。`soldout: true` ＋ `saleEnded: true` ＋ `saleEndedSince`。バッジ「**販売終了**」（点線） |
| **公演が終わった** | ✅消す（公演当日は残して**翌朝**消す） |
| **どの売り手にも枠が無く、公演も終わっている** | ✅消す |

🚨**消すと「売り切れた」のか「最初から載っていなかった」のか利用者に区別できない**＝サイトの信用が落ちる。
これはユーザー本人の方針転換（2026-08-09・大相撲九月場所の実体験）。
安全弁＝**公演日を過ぎたら自動で消える**ので、永久に居座ることはない。

- 判定は🚨**その枠の売り手の実ページ**で。`ticket.url` が e+ なら e+ を見る。
  ぴあは在庫を持たないので「ぴあに予定枚数終了が出ていない」は根拠にならない。
- 裏が取れない枠は**弱いほう＝販売終了**に倒す。「売り切れた」という強い主張を裏取りなしでしない。
- **エントリ単位で一括マークしない**。同じエントリに売り切れた枠と生きた枠が混在する。

---

## 2. 「買える枠ゼロ」を見たら、消す前に必ず疑う6つ

**この6つを全部潰すまで削除候補に出さない。**

1. **ぴあがeventCdを無効化した**（HTTP200のまま「ご指定の公演情報が見つかりませんでした」）
   → `python tools/pia_kw_search.py "<アーティスト名>"` で後継のbundleを探す
2. **w.pia.jp 直販形式**（券種カードが1枚も無い）→ 終了枠すら0は異常のサイン。**機械で判断せずユーザーに見せる**
3. **sorry（混雑）ページ**を掴んでいる
4. **発売時刻の前**（当日発売の枠は時刻前だと0枠に見える）
5. **他社で売っている**＝e+ `/sf/word/<id>`・楽天・ローチケ・主催直販
   🚨**「ぴあで0枠」は削除理由にならない**。ぴあ枠が即完しただけ／主催直販／他社独占が普通にある
6. **まだぴあにページが立っていない**（発売前・延期で作り直し中）
   → Pコード/Lコードが公表されていれば、そこで売ることは確定している

🚨**主催者を特定しないまま「大手に無い＝無い」と結論しない。**
地方の自主公演は主催団体のサイトにしか情報が出ない。会場の公式カレンダーは貸館公演を載せないことがある
＝**載っていないのは不在の証拠にならない**（2026-08-20 榛葉樹人＝削除後に復活させた）。

### 「生きて見えた」のに死んでいた偽シグナル4つ

| 偽シグナル | 実態 | 見破り方 |
|---|---|---|
| 主催公式「NOW ON SALE」 | 静的表示が残っているだけ | 同ページに**リセール案内**があれば完売を疑う |
| e+一覧の「先着一般発売」 | **券種名**であって販売中ではない | `/sf/detail/` の**個別ページ**を開く |
| ぴあkw検索の「[一般発売]」 | 販売区分のラベル。在庫の有無ではない | 検索結果を在庫判定に使わない |
| 公式「M/D 10:00 開始予定」 | 発売前に作った静的ページが未更新 | 発売日を過ぎたら実ページを見る |

✅**最終判定はいつも「ぴあ実ページ `t.pia.jp/pia/event/event.do?eventCd=` の券種ステータス」**。
**リセール導線しか無い＝完売。**

---

## 3. 自動削除の除外リスト（＝保留に回す・自走で消さない）

- 売り切れ／予定枚数終了 → `mark_soldout.py` で表示側へ
- 本日発売で**発売時刻より前**
- w.pia.jp 直販で券種0
- 公演日が未来で**他社に生き枠**
- 抽選結果発表待ち
- **照合がぴあだけ**（他社を見ていない）
- `saleEndUnknown: true`（削除候補に出さず「⚠️要再確認」へ）

🚨**候補を出すときは「ぴあのみ照合済み・他社未確認」と必ず添える。**添えないとユーザーが全部OKしてしまう。

---

## 4. 手順

1. `python tools/check_expired.py` → 削除候補と「⚠️要再確認」を出す
   （公演日が未来のものは機械判定だけで候補にしない仕組みが入っている）
2. `node tools/check_zero_badge.js` → **カードは出るのに買える枠0**を結果側から数える
   （終了コード 2＝要対応／1＝30日以内のみ／0＝健全）。2なら放置しない
3. 候補を**二段構え**で検証
   - `python tools/reconcile_pia.py --ids <ids>` で独立再照合
   - `python tools/check_badges.py`
   - ぴあ以外は**実ページの販売期間の文字**を読む（開始が過去かつ終了が未来なら販売中＝削除NG）
   - 🚨WebFetchの「終了しました」を鵜呑みにしない。**日付で判断する**
4. **別エージェントに「削除は誤りという前提で」独立再導出させる**（候補値を見せない）
5. バックアップ＝`index.html.bak_MMDD_del<id>`
6. 削除を実行 → **`logs/removed_YYYY-MM-DD.md` に公演名＋確認用の直URLを残す**
7. 報告にも同じ一覧を出す。URLは🚨**index.html から機械抽出**（手で書かない・eventCdを創作しない）

### 候補の見せ方

- 検索ワードでなく**確認用の直URL**（売切組はサイト表示から消えるので検索では出てこない）
- 長いURLは `[短ラベル](URL)` 形式
- **2群に分ける**＝「✅確認済み（参考URL）」と「👉要チェック」。
  裏取り済みを要チェックに混ぜない＝ユーザーが全部開き直す二度手間になる
- 訂正するときは**差分でなく決定版の全リストを出し直す**。表を2つ出さない

---

## 5. ヒールを走らせたら必ず突合する

🚨`heal_stale_deadlines.py --apply` の安全弁は**「公演単位」でしか比べない**ので、
**同じ公演の券種違いを丸ごと消しても気づかない**（2026-09-01＝阪神×広島9/17が12枠→1枠）。

```python
# today は当日。soldout / saleUntilSoldOut は常に表示。
def visible(t):
    if t.get('saleUntilSoldOut') or t.get('soldout'): return True
    sd, d = t.get('startDate'), t.get('date')
    return not ((not sd or sd <= TODAY) and d < TODAY)
# git show HEAD:index.html と現物で、エントリごとに visible の数を比べる
```

⚠️券種名が「9/1 10:00発売」→「〜12/20 23:59」に書き換わっただけの枠を「消えた」と数えない
（日付部分を落として突き合わせる）。
🚨消える前に**statustextで「予定枚数終了」か**を確かめる。売り切れなら消さずに `soldout` を付ける。

```python
# state は「予定枚数終了」と「受付終了」を同じ"受付終了"に潰す。生HTMLの文言を読む
m = re.search(r'__status (is-[\w-]+)">(.*?)(?:<br|</p>)', item_html, re.S)
```

🚨**ヒールや統合の直後に常設ツール（dedup_badges 等）を流さない**＝直した内容を壊す。

---

## 6. 日付まわりの決まり

- **販売終了日が公演日より後**なら、その公演の枠は**公演日で締める**
  （複数公演まとめのreleaseは最終公演基準の締切が出る）
- **販売終了日が不明**なら `date = startDate` ＋ `saleEndUnknown: true`。
  🔴**推測で日付を入れない**（「公演前日だろう」は重大違反）。発売開始日にユーザーへ確認する
- **公演当日・当日券が残る枠は当日中は残す**＝翌朝削除
- 削除したものの**販売再開は追わない**。たまたま見つけたら再掲載でよい

---

## 7. 起きた事故（同じ道を通らないための一覧）

| いつ | 何が起きたか |
|---|---|
| 2026-05-21 | JEAN RENO静岡に**推測で日付**を入れた（ユーザー激怒）。しかも実は完売で載せてはいけない枠だった |
| 2026-05-29 | 機械判定だけで販売中3件を削除しかけた（ticket.dateは延長されている） |
| 2026-06-23 | 公演が未来の私立恵比寿中学を候補に入れた。枠は**一日の中で次々開く** |
| 2026-06-26 | `rlsCd=` 形のURLで「0枠」と誤判定（`event.do?eventCd=` で見たら受付中） |
| 2026-07-05 | 楽天のみのエントリをWebFetch誤読で候補に出した（機械照合が効かない） |
| 2026-07-13 / 08-01 | 候補一覧に**eventCdを創作**して貼った（2回）。URLは機械抽出のみ |
| 2026-07-15 | w.pia直販48券種を「0枠」と読み、その日の主役を消しかけた |
| 2026-07-18 | reconcileのDROP「解析不能」を数一致だけでノイズ扱いした（締切延長の隠れ枠） |
| 2026-08-01 | 昼ヒールの候補11件を他社確認＝**ぴあだけ見ていたら9件の誤削除**に見えた（最終的には全件死亡） |
| 2026-08-12 | ぴあがeventCdを無効化 → 購入ボタンが無言で死に、削除候補に化けた |
| 2026-08-14 | e+の枠をぴあと突合して、売り切れ3枠を「販売終了」に格下げ＝新しい嘘を作った |
| 2026-08-20 | `mark_soldout --review --apply` が**e+枠の売り切れ表示を大量に外した**（git diffで発見・巻き戻し） |
| 2026-08-20 | 「大手のどこにも無い」で削除 → 主催サイトに発売告知があり復活 |
| 2026-08-26 | ぴあ0枠の仙台貨物を消しかけた（e+にツアーの続きが生きていた） |
| 2026-09-01 | 昼ヒールが阪神×広島の**12枠を1枠に**（残ったのはビジター席だけ） |

**共通の型＝「1つの売り手・1つの道具・1つの画面」で結論を出した時に必ず外している。**
