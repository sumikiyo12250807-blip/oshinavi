# OSHINAVI作業プラン

毎朝の作業開始時、必ずこのファイルを先に確認してください。
🧭 **何か作業する前に必ず `PLAYBOOK.md` の該当行を見る → 挙がった memory を全文Read → 動く**（一行要約で動かない）。

---

# ✅ 9/4 朝の便＝完了（06:28 push・未pushコミット0）

**index 4,366件／新着プール51件／push 1回目（朝）を消化**

## 🚨 ユーザーの返事待ち＝2件（次に話すとき最初に聞く）

### ① C型72組＝同じ枠に「個別ページ」と「ツアーまとめページ」の2枚が並んでいる

```
id505 石若駿トリオ「一般発売（新潟 10/18公演）〜10/12」
   → t.pia.jp/.../event.do?eventCd=2618035           ← 公演ごとの個別ページ
   → ticket.pia.jp/.../event.do?eventBundleCd=b2670733 ← ツアーまとめページ
```
**72組・21エントリ**。飛び先が違うので [[feedback_dedup_badges_keeps_urls]] の
「飛び先が違えば畳まない」に当たる可能性があり、**手を付けていない**。
選択肢＝**1 個別を残す（あたしの推し）／2 まとめを残す／3 両方残す**。
道具は用意済み＝`python tmp/fix_dup_c_0904.py [--keep bundle] [--apply]`（既定は個別を残す）。
一覧＝`python tools/check_dup_slots.py`（終了コード1＝Cのみ）。

### ② id6397「Good Morning Record Bar Presents『GOOD DAY KYOTO』」のジャンル
ぴあは「音楽/海外ROCK・POPS」。出演者に **Luci Gang(from Korea)** が1組いる。
[[feedback_kpop_vs_yougaku]]は「韓国のアーティストなら kpop に読み替える」だが、
**複数組のイベントで1組だけ韓国**の場合をどう扱うかが決まっていない。
→ **yes（kpopにする）/ no（yougakuのまま）**。
この1件だけ振り分けずプールに残してある（新着プール51件のうちの1件）。

---

## ✅ 9/4 朝にやったこと

| やったこと | 結果 |
|---|---|
| **二重登録の解消** | **123枠**（76エントリ）。同じバッジが2枚並んでいたのを畳んだ |
| 前夜の新着69件を照合 | OK65・取りこぼし2件を回収（6412 ハムレット／6410 デカ×ホシ） |
| 隠れ枠ヒール | 162件走査→**53件適用**。画面に出る枠 7421→7430（**減少0**） |
| 期限切れ削除 | **6件**（`logs/removed_2026-09-04.md`） |
| **ツアー分裂の回収** | **74枠を84エントリに追加**（`logs/merged_2026-09-04.md`） |
| 千秋楽の修正 | **23件**（放置すると足した枠が画面から消えるところだった） |
| ジャンルの修正 | 既存6件（→talkshow）＋プール4件 |
| 振り分け | **68件**（`logs/assigned_2026-09-04.md`） |
| 新着収集 | **50件投入**（id6501-6550・全件が発売前） |
| 保留分の回収 | 6401 藤原大祐／6402 fripSide／1772 柳家喬太郎 に枠を追加 |

### 🆕 作った番人＝`tools/check_dup_slots.py`（毎朝回す）
「同じエントリに実質同じ枠が2枚ある」を**結果側から**数える。
既存のどの道具でも拾えなかった型（`dedup_badges` は url を判定キーに含むので
「url無し版とurl有り版」を畳まない／`reconcile --new` は新着だけ／`check_zero_badge` は枠0だけ）。
終了コード **2＝A/Bあり要対応 / 1＝Cのみ / 0＝健全**。

---

## 🚧 積み残し（優先度順）

| 何 | 中身 |
|---|---|
| **統合先の取りこぼし24件** | `tmp/recon_merged_0904.txt` の MISSING。**天満天神繁昌亭が8件**（落語の定席で毎日公演）、藤澤ノリマサ・斉藤和義のプレリザーブなど。**昼便で回収する** |
| **STALE 4件の売り切れ判定** | 617 因幡晃(神奈川10/11)／738 フラワーカンパニーズ(青森・岩手)／1772 柳家喬太郎(愛知9/25)／2121 春風亭一之輔(神奈川10/29)。締切は未来なので画面には出ている。**2121だけ ticket.url が空**＝reconcileがそのページを見ていない偽陽性の疑い。`tmp/check_stale_urls_0904.txt` |
| **見出しラベル9件** | 608/620/1119/3489/5364/5550/5560/5656/6065。会場を足したのに `dateLabel` が古い形（「9/5(土)茨城／9/13(日)愛媛」形・曜日なし形）で機械置換すると壊れるので**date だけ直して表示は据え置き**。`tmp/label_built_0904.json` に正しいラベルの候補あり |
| **バッジ0の要対応66件** | うち59件が「先行だけ登録・一般発売がまだ出ていない」型。ぴあに枠が出れば翌朝の照合で拾える |
| **未投入 fresh 17件** | 今朝の fresh 67件のうち50件を投入した残り。`tmp/_triage_0904.json` |
| **千葉ロッテ〈ワイドシート〉** | プール6425と座席種違い。次のバッチで新規投入する。`tmp/newbatch_held_0904.json` |
| **同名既存117件** | 今朝の `tmp/_triage_0904.json` の samename。9/3ぶん90件は今朝回収済みなので**今日ぶんの新規分**。統合の道具は `tmp/samename_*_0904.py` 一式が使える |
| **発売日不明32件** | `tmp/_triage_0904.json` の unknown |
| id4491 石川さゆり（三重1/30）のぴあURLが死んでいる | 9/12が発売日。**9/12に再確認**（消さない） |
| id41 と id450 のプリキュア二重登録 | 41（長野9/5・枠が死んでいる）は450のツアー束ねに含まれる |
| id3114 上田正樹 | 9/4公演の枠があるので今日は消さない。**明日9/5の朝に削除** |
| id6437 佐久間宣行PRESENTS | 当日引換券がぴあから消えた（単独リトライ2回でも0枠）。**消さずに保留** |
| e+の未登録 残り100件 | `tmp/_word_pool_miss_0901.json` |
| ⏳同日同会場の昼夜公演にバッジの開演時刻 | 6274石川さゆり/6275/6298/6305/6307/6308 ほか |
| 🗂memory棚卸し 区画2・3 | 次は「ぴあの道具11本」→「収集・新着15本」 |

---

## ☀️ 昼の便（アラーム 15:05 を仕掛け済み）

今日の当日発売は **10:00 / 11:00 / 12:00 / 15:00 / 20:00**。15:00発売の後に回すため **15:05** にアラーム。
（**20:00発売ぶんは 20:05 のアラーム**で別途ヒールする。どちらも背景の sleep で仕掛け済み）

```
1. python tools/heal_stale_deadlines.py --build → --apply
   → tmp/heal_diff_0904.py の BEFORE を新しいバックアップ名に変えて枠数突合
2. 上の「統合先の取りこぼし24件」を回収（天満天神繁昌亭ほか）
3. node tools/check_zero_badge.js / python tools/check_dup_slots.py
4. push（昼の1回目）
```

## 🌙 夜の便

**着手は17:00**。X投稿＝トレンド枠0〜1本＋主役枠1本＋ジャンル別まとめ。
文面は Fable に `X_SCRIPT.md` 1枚＋その日の素材だけ渡す。予約は **20:01から15分おき**。
予約後に「投稿に出した公演のぴあ総ざらい」→直してから push。

---

## ⚠️ 9/4 朝の反省と気づき

1. ✅**朝いちで背景アラームを2本仕掛けた**（15:05・20:05）。9/3の反省（昼便を17時まで落とした）への対策を実行できた。
2. 🚨**ヒールの `--build` は出力がバッファに溜まって20分以上「0バイト」に見える**。
   CPU時間だけ見て「1件50秒＝2時間半かかる」と誤診し、ユーザーにもそう報告して訂正した。
   **実際は13秒/件で162件を35分**。→ **進捗は `grep -c "^\[" ログ` で数える**（バッファが吐かれた後なら見える）。
   止める判断をする前に、まず**単発で1件叩いて応答速度を測る**（`tmp/pia_probe_0904.py`＝0.7秒で正常だった）。
3. 🚨**`parsed < total` は取りこぼしではない**（同じ公演が複数行に出るのを URL で重複排除した結果）。
   道具のコメントに明記されているのに、先に「315件が取れていない」とユーザーに報告してしまった。
   **打ち切りの検知はページ到達率（`fetched_pages >= pages`）で見る**。
4. 🚨**triage の同名判定は「既存エントリ（genre != new）」しか見ていない**＝
   **まだ振り分け前の新着プールと同名のものが素通りする**。今朝4件を選定側で落とした。
   道具に入れるなら `tmp/select_batch_0904.py` の「プールと同名で保留」ブロックが雛形。
5. 🚨**統合で会場を足したら `entry.date`（千秋楽）を必ず更新する**。
   忘れると `ev.date < 実際の千秋楽` になって**足した枠が画面から消える**。
   気づけたのは `reconcile_pia --ids` の **QC-EVDATE** のおかげ。統合したら必ず --ids で照合する。
