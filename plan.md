# OSHINAVI作業プラン

毎朝の作業開始時、必ずこのファイルを先に確認してください。
🧭 **何か作業する前に必ず `PLAYBOOK.md` の該当行を見る → 挙がった memory を全文Read → 動く**（一行要約で動かない）。

---

# ✅ 9/3夜のX投稿5本＝**予約完了**（19:47・Chrome拡張で実施）

`x.com/compose/post/unsent/scheduled` で5本とも時刻を目視照合済み。

```
20:01 POST1（主役枠・ガールズバンドクライ）
20:16 POST2（クラシック）
20:31 POST3（JPOPなどの音楽）
20:46 POST4（お笑い・落語）
21:01 POST5（舞台・スポーツ・展示など）
```

### 🎯 Chrome拡張でXを操作する手順（9/3 実地で確立・**PowerShellより速くて確実**）
- 🚨**拡張のブラウザツールが降りる条件＝`claude --chrome` で起動すること**。ただの再起動・`/clear`では降りない。
- 🚨🚨**座標でクリックしない＝`read_page`で取った `ref` を使う**。拡張のスクショはページ実サイズと縮尺がずれるので、
  座標クリックは別の場所に当たる（19:25に本文欄を外して打鍵が丸ごと消えた）。**refなら一発で当たる**。
- 手順＝①`navigate` で `x.com/compose/post` ②`read_page --interactive` で
  `textbox "Post text"` と `button "Schedule post"` の ref を取る ③本文欄の ref をクリック→`type` で本文
  ④`read_page ref_<本文欄>` で**本文を全行照合**（貼れたか目視でなく機械で見る）
  ⑤`Schedule post` の ref を**2回クリック**（1回目はホバー扱い＝従来と同じ癖）
  ⑥Minute/Hour は `form_input` でセット（既定は現在時刻+1時間）→ `zoom` で「Will send on ...」を読む
  ⑦Confirm の ref → 最後のボタンの ref を `read_page --all` で読んで**"Schedule"の文字を確認**→2回クリック
- ⚠️`type` は長文で**30秒タイムアウトのエラーを返すが、実際は入っている**。エラーが出たら打ち直さず、
  まず `read_page` で中身を確認する（打ち直すと二重になる）。
- ⚠️拡張から見えるのは**そのセッション専用のタブグループ**だけ。新しいウィンドウが1つ開く。Xのログインは引き継がれた。

### ⛔【失効】Chrome拡張の状態（9/3 19:05 実測・19:13に上のブロックへ置き換わった）
- 拡張は**Chromeに入っている**（`elifhakcjgalahccnjkneoccemfahfoa` ほか／窓のタイトルも `Claude in Chrome`）。
  ただし画面は**まだベータの案内ページ（「デモを試す」ボタン）**で止まっている。
- 🚨**`/clear` ではツールは降りてこない**（ToolSearchを3通り＝browser/mcp/dom系で叩いて0件・19:05実測）。
  **Claude Code 本体を立ち上げ直すしかない**。ちょうど `Update installed · Restart to apply` も出ている。
- ⚠️立ち上げ直すと**背景アラームも消える**＝新セッションでいちばん最初に下の20:05アラームを仕掛け直すこと。
- 拡張が使えなくても**予約はできる**＝PowerShellで撮る/押す従来ルート（[[feedback_x_browser_operation]]）。
  20:01に間に合わないと判断したら拡張は捨てて従来ルートで押す。

### ✅ 20:05 のヒール＝実施済み（20:12）
- ⚠️**plan.mdの「20:00の鼓童×初音ミク」は誤り**。鼓童（id4113）は **9/4 20:00発売＝明日**。
  今日いちばん遅い当日発売は **20:00の id6434 acosta! と id6435 映画名探偵プリキュア×TOWER RECORDS** だった。
  🚨**「いちばん遅い当日発売」は思い出しでなく `tmp/today_flat_0903.py` で機械で出す**（startDate==date==today の type から発売時刻を抜く）。
- `--ids 734,3370,3735,3752,5516,5641,6434,6435 --build → --apply`
  ＝**3件適用**（734 福田こうへい／6434 acosta!／6435 プリキュア）・発売日引き継ぎ6枠。
- **枠の増減ゼロ**（画面に出る枠 7677 → 7677・`tmp/heal_diff_0903_2000.py`）／`check_order.js` 並び順違反0。

### 🚨 明日の朝に持ち越した4件＝**枠が「url有り／url無し」で二重登録されている**
`heal --apply` の安全弁が毎回ブロックする。**混雑ページではなく、データ側の二重登録が原因**。

| id | 公演 | 中身 |
|---|---|---|
| 3370 | ネパールの詩心 | 4公演ぶんが**個別eventCd版**と**bundle(b2670612)版**で計8枠 |
| 3735 | パパイヤ鈴木 | プリセール・一般が url無し／url有りで各2枠 |
| 3752 | 爆笑ファンタジスタ | 同上 |
| 5516 | Golden Moon Concert | 一般発売が url無し／url有りで2枠 |

- ぴあ側は**正しく締切を返している**（例 3735＝〜11/3 23:59）。ブロックの理由は
  build が返す枠に url が付かず（[[feedback_build_pia_multiurl_loses_ticket_url]]）、
  **url有りの枠が「消える」と判定される**から。
- 🚨**放っておくと明日、この枠は隠れ枠として画面から消える**（startDate==date==9/3 になっているため）。
- ⚠️**畳み方はユーザーに聞くこと**＝3370は飛び先URLが2種類（個別／bundle）あり、
  [[feedback_dedup_badges_keeps_urls]]「飛び先が違えば畳まない」に当たる可能性がある。

### ⚠️ id5641『5秒で完全犯罪を生成する方法』公開記念舞台挨拶＝買える枠ゼロ（7枠とも）
9/3 10:00発売・9/10〜9/12公演。**即日完売の疑い**＝削除せず保留。
🚨判定は生HTMLの `__status (is-xxx)">` の文言で「予定枚数終了」か「販売終了」かを見てから
（[[feedback_soldout_keep_visible]]／[[feedback_saleended_vs_soldout]]）。

---

## 状態（9/3 20:20）

**index 4,322件／新着プール69件／push 3回とも消化（朝07:41・昼18:14・夜19:00）**

🚨**未pushコミット 2本**（`git log origin/main..HEAD`）
1. `c4d95930` 新着タブの不具合修正（**本番のoshinavi.jpはいま壊れたまま**）
2. `5749f06c` 20:00発売ぶんのヒール

→ **4回目のpushをしてよいかユーザーに聞いている（返事待ち）**。NGなら明日の朝いちのpushで出る。

## 🌅 明日9/4の朝いち

```
1. 🚨まず「その日いちばん遅い当日発売の5分後」に背景アラームを仕掛ける
   （今日それを忘れて昼便を17時まで落とした。仕組みが無いと必ず落ちる）
2. python tools/check_expired.py
3. python tools/heal_stale_deadlines.py --build → --apply → tmp/heal_diff で枠数突合
4. node tools/check_zero_badge.js
5. 🚨**二重登録4件（3370/3735/3752/5516）を先に片づける**＝上の「明日の朝に持ち越した4件」。
   放っておくと今日発売の枠が画面から消える
6. 前夜の新着チェック → 振り分け → 新着収集 → 最後に push
```

### 明日チェックする新着＝**69件**
- **ぴあ64件**（9/3朝投入・id6394-6468の一部）＝`reconcile_pia.py --ids` ＋ 別エージェントのゼロ再導出 → 振り分け
- **9/3夜に足した5件**（id6477/6478/6481/6484/6486）＝取りこぼし監査から。
  🚨**名前を出演者名から正式イベント名に直して入れてある**ので、再チェック時に名前を戻さないこと。

## 🚧 積み残し（優先度順）

| 何 | 中身 |
|---|---|
| **朝の統合候補11件** | `tmp/_merge_pending_0903.json`。6406市川團十郎(見切れ席)→4279／6407髪結いの亭主→3551／6413府中の森笑劇場→2121／6417ALICE岐阜羽島→2239／**6418プリキュア石川→450**／6432HelloKitty長崎→2362／6436レオてつ和歌山→4223／6438しまじろう滋賀→309／6444秋川雅史(当日引換券)→2471／6450N響第9→4843／6456JapanHornSound名古屋→4291<br>🚨6432と6436は既存側に同じ枠が既にある＝新側を捨てる形 |
| **同名既存95件** | `tmp/_triage_0903.json` の `samename`。八神純子3・荒谷翔大2・因幡晃2など＝ツアー分裂の疑い |
| **バッジ0の要対応65件** | うち大半が「先行だけ登録・一般発売がまだ出ていない」型。ぴあに枠が出れば翌朝の照合で拾える |
| **ヒールの安全弁ブロック15件** | 正体＝**1エントリが複数のeventCdの枠を持っていて、ヒールが代表URL1本しか見ない**ため「消える」と判定される（id579で実測）。安全弁が守るのが正しい。道具を直すなら「全ticket.urlを見る」形へ |
| **統合待ち71件** | `tmp/_newsame_0902.json`（9/2朝のスイープ分） |
| id4491 石川さゆり（三重1/30）のぴあURLが死んでいる | 9/12が発売日。**9/12に再確認**（消さない） |
| id41 と id450 のプリキュア二重登録 | 41（長野9/5・枠が死んでいる）は450のツアー束ねに含まれる。6418の統合と一緒に整理 |
| e+の未登録 残り100件 | `tmp/_word_pool_miss_0901.json` |
| ⏳同日同会場の昼夜公演にバッジの開演時刻を入れる | 6274石川さゆり/6275/6298/6305/6307/6308 ほか。**枠は1つで正しい**が時刻の区別が要る |
| 🗂memory棚卸し 区画2・3 | 次は「ぴあの道具11本」→「収集・新着15本」 |

## ✅ 9/3 にやったこと

- **朝**＝削除10件／ヒール35件（枠の減少0）／売り切れ71件判定→削除0・10枠に印／
  ぴあ87件を独立再照合＋エージェント2本でゼロ再導出→**87件を振り分け**／発売前を総ざらいして**64件投入**
- **昼**＝ヒール41件で本日発売の枠に締切を取り込み（発売日引き継ぎ39枠・枠の減少0）
- **夜**＝X投稿5本を用意（ユーザーが2本を直し、残り3本をその型に合わせた）／
  **投稿に出した72名義でぴあを総ざらい→24件の取りこぼしを発見→12枠を既存に回収＋新規5件を投入**
- **ユーザー依頼**＝キッズのAmazonボタンをユーザー作の塗り絵に差し替え（`KIDS_AMAZON`・ラベル「ぬりえ」）／
  **「トークショー」ジャンルを新設**（ぴあの「講演会・トークショー」の行き先を engeki から独立。既存9件を移動）／
  9/2から保留のe+10件を振り分け（学園祭7件＋jpop/classic/musicetc各1）

## ⚠️ 9/3 の反省

1. 🚨🚨**昼のヒールとpushを17時まで落とした**。原因は「時計を叩かない」で、
   **対策（朝いちで背景アラームを仕掛ける）を今朝やらなかったこと**。8/21・8/22と同じ穴。
   → **朝の手順の1番目にアラームを置いた**（上の「明日の朝いち」）。
2. **「売り場コードのユニーク数＝枠数」は受付中の枠にしか使えない**。発売前の枠はリンクを持たないので
   1つも数えられず、それでも「登録1枠＝ユニーク1」に見える。**生HTMLの販売スケジュール行を数える**
   （雛形 `tmp/raw_slots_0903.py`）。memory feedback_pia_parser_flattens_slots に追記済み。
3. **エージェントの「枠が2つある」は公演回を数えていることがある**＝枠（売り場）とは別物。鵜呑みにしない。
4. **取りこぼし監査で出た名前は「出演者名」のことがある**。9/3は5件中5件がそれで、
   うち1件（さだまさし＝笑う朗読3）は**本人が出演すらしない**。名義のまま入れたら嘘になる。
   → **監査で拾った新規は、必ず実ページで正式イベント名を確かめてから投入する。**

## 🆕 9/3 に決まったこと（X投稿）

`X_SCRIPT.md` に追記済み。
- **サイトの説明とURLを本文の前半に置く**（末尾のCTAはそのまま）。「無料」と言い切り、「あなたの推しもいる」と寄せる
- 予告とフォロー促しは**1文に統合**
- 指示（「目を通してみて」）でなく**読む人の楽しみ**で書く
- 🚨**OSHINAVIから売り場へ案内できないもの（グッズ等）は詳しく書かない**
  （ユーザー「グッズの売り場までは、OSHINAVIからいけないからこのままでいい」）
